const names: string[] = ["anis", "molla", "zahid"];
const numbers: number[] = [1, 2, 2, 3, 3, 4, 4, 4, 5, 6, 6];

names.map((name) => console.log(name.charAt(1)));
numbers.map((number) => console.log(number));
const user: [number, string] = [1, "anis"];

console.log(user);
